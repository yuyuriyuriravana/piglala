Supported commands:

`!help`
Show this command list.

`!play <YouTube URL>`
Join your current voice channel and play the requested video. A new request replaces the current track.

`!stop`
Stop playback and leave the voice channel.

`!status`
List watched players and their recorded best parses for the latest Savage tier plus any Ultimates they have done.

`!price <item name>`
Compare the cheapest market listing across global regions and get a short price-history observation.

`!subscribe`
Receive one notification for each newly observed high-end fight result.

`!unsubscribe`
Stop receiving parse result notifications.

`!watch <name> <server> <region>`
Start tracking a player.

`!unwatch <name> <server> <region>`
Stop tracking a player.
